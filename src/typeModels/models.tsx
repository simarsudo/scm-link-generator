export type sticker = {
	name: string;
	type: string;
	isTS: boolean;
	tournamentName: string;
	grade: string[];
};

export type gun = {
	name: string;
	type: string;
	isStatTrak: Boolean;
	conditions: string[];
};
